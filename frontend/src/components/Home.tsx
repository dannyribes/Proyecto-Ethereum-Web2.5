import React from "react";
import { Typography } from "@mui/material";

const Home = () => (
  <div className="mx-3 my-5">
    <Typography variant="h3">Welcome to Ethereum Project</Typography>
    <Typography variant="h4">by Victor, Daniel and Guille</Typography>
  </div>
);
export default Home;
